一、在实际工作中，都有一个团队在做项目，不是一个人在工作，多人协作，就是每个人都有自己的命名习惯。

1、css选择符命名，规范

2、命名规范文档。

二、css选择符：
css选择符作用？指定css样式所作用的对象【范围】

1、标签选择符：针对html标签
2、id选择符：针对页面中只出现一次的内容，id
3，类选择符：针对某些元素，相同的样式，重复样式

*******默认情况某些html元素本身有自己的默认值

4、控制所有元素----使用通配符----*

通配选择符：*{属性：值}，用于定义所有的html元素
*******通配符的作用范围很广，但是效率最低，慎用。

	*{margin：0；padding：0；}//重置所有元素的，外边距与内边距为零。

5、选择符的嵌套使用-----包含选择符
   前端开发的极限-----就是代码非常精简

6、多个选择符，会有一些相同的样式，这个时候，我们就可以，把这些选择符，组成一个组
选择符1，选择符2，。。{属性：值}

*****一般程序喜欢把注释写在代码的上边。

********css选择符就是表示你将把你定义的样式用给谁。

下面选择符表示，这些选择符分为一组，他们都有相同的样式
body ul ol li p h1 h2 h3 form fieldset table.top a , .top 

表示针对类名为top里面的a
.top a , .top a:visited


简单魔方新浪页面第24将，综合运用！

俩个相同的标签，其中一个带有id，那么带有id的标签中，会带有未带有id标签的属性么？

三、css盒子模型

****一个盒子有以下几部分构成

1、盒子中的内容content
2、盒子的边框 border
3、盒子的边框与内容之间的距离，称为填充---padding，内边距（内补丁）
4、多个盒子存在盒子与盒子之间的距离称之为边界---margin，外边距（外补丁）

*******就是盒子所在的实际宽度为例=左右边界+左右边框+左右内填充+内容宽度

*****css盒子的相关属性

【1】、内容属性：内容本身的宽：width ，内容本身的高 height
【2】、内填充属性：内容与边界之间的距离padding
****再定义盒子的宽度是，要考虑到，内填充，边框、边界的存在。
如果增加了内填充，整个盒子的宽度值要减去你所增加的内填充值。

盒子 上=20 右边=30 下=50 左边=100
margin 20px 30px 50px 100px
margin-top（right，bottom，left）

*****使用外边距要注意浏览器的兼容性
******除了值为0的情况下，所有的非零的值，后面的都要加单位。

*****由于各个浏览器存在着内外边距默认值不同。我们需要将所有浏览器的默认内外编剧从零开始计算（归0）。

实际工作，不要使用*{margin：0；padding：0}，这样可以，但是效率很低，应该用到哪些元素，把哪些元素默认值归零。

css的属性，虽然有继承的特点，但并不是所有的元素都有继承。

css布局主要是通过合资模型来实现---w3c将网页内容放置在一些盒子中，并对合资的一些属性进行控制。
 内容：width height
内填充 ：padding
边框：border
外边界：margin

关于列表的属性介绍

list-style：列表的图片

语法：
list-style：list-style-image||list-style-position||list-style--type

列表的样式：列表的图片 列表的符号位置 列表的样式

list-style：none 不要列表符号。

边框的属性说明：

css语法： 
border：border-width||border-style||border-color

根据语法，推理出它的使用方式

如果只写border，则后面要跟的是三个不同的字属性：粗细 样式 颜色
边框样式：俩个像素粗细  实现  蓝色
border：2px solid blue

******css小技巧：
【1】、让盒子水平居中，将对象左右边界设置为auto；
【2】、让盒子中的内容垂直居中，设置行高=盒子的高度，但是不要有换行；
【3】、我们在调试的时候，可以适当的加背景颜色。

**********html元素的分类：块和内联

背景图片：默认情况下是进行水平与垂直方向上的平铺。
默认背景图片在元素的左上角显示。
背景图片的依附方式：固定的含义是将图片固定在屏幕（可视区域）的某个位置。
兼容性 IE6只有html和body俩个元素支持该属性值。

翻转效果，其实思路就是准备俩张大小相同，内容不同的图片，正常情况下显示一张，在鼠标经过的时候显示另一张。

伪类表示一种状态（：link）

css精灵技巧主要是为了减少http请求。


*******取消无需列表中的点，可以使用列表样式属性！
也可以使用display属性，取消或者添加某个元素的块属性。



