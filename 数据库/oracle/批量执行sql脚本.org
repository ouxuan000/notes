------ windows ------
+ 方法一：
>copy *.sql all
>ren all all.sql
>sqlplus aa/bb @all
>所有的文件内容放到了一个文件中
 
+ 方法二：
>脚本文件目录下，
>dir /b *.sql > aa.sql
>然后用UE的列编辑模式，给行头都加上@，然后再执行。
>或者使用vim编辑器,vim 每行行首或行行尾插入,每行的行首都添加一个字符串：%s/^/要插入的字符串,每行的行尾都添加一个字符串：%s/$/要插入的字符串
 
>% 代表针对被编辑文件的每一行进行后续操作
>$ 代表一行的结尾处
>^ 代表一行的开头处